<!DOCTYPE html>
<html lang="en">
  <head>
      <% include ../partials/head %>
  </head>
  <body>
    <%- include('../partials/nav', { active: 'index' }) %>
    <div class="container">
      <div class="row justify-content-center">
        <div class="col-xs-12 col-sm-8">
            <h1 class="hft-sb-title display-5">Messages</h1>
        </div>
      </div>

      <div class="row justify-content-center">
        <div class="col-xs-12 col-sm-8">
          <div id="hft-shoutbox-alert" class="alert alert-danger d-none" role="alert">
            😱 THERE ARE NOT ENOUGH CHARS IN YOUR FORM FIELDS
          </div>


        </div>
      </div>

      <div class="row justify-content-center">
        <div class="col-xs-12 col-sm-8">
          <table class="table table-striped">
            <thead>
              <tr>
                <th scope="col">#</th>
                <th scope="col">Name</th>
                <th scope="col">Message</th>
              </tr>
            </thead>
            <tbody>
            <% shouts.forEach((row) => { %>
              <tr>
                <td><%= row.id %></td>
                <td><%= row.username %></td>
                <td><%= row.message %></td>
              </tr>
            <% }); %>
            </tbody>
          </table>
        </div>
      </div>
      <div class="row justify-content-center">
        <div class="col-xs-12 col-sm-8">
          <form id="hft-shoutbox-form" method="post" action="/add-entry">
            <div class="form-group">
              <label for="hft-shoutbox-form-input-name">Name</label>
              <input type="text" name="username" class="form-control" id="hft-shoutbox-form-input-name" placeholder="John Doe">
            </div>
            <div class="form-group">
              <label for="hft-shoutbox-form-input-text">Example textarea</label>
              <textarea class="form-control" name="message" id="hft-shoutbox-form-textarea" rows="3"></textarea>
            </div>
            <button id="hft-shoutbox-form-submit" type="submit" class="btn btn-primary">Submit</button>
          </form>
        </div>
      </div>
    </div>
  <footer>
      <% include ../partials/footer %>
  </footer>

  </body>
</html>
